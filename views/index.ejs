<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content"width=device-width, initial-scale=1, maximum-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Chuck Norris Joke Application</title>
    <link href="https://fonts.googleapis.com/css?family=Nunito+Sans:400,700" rel="stylesheet">
  </head>
  <body>
    <style>
      html {
        font-family: 'Nunito Sans', sans-serif;
        font-size: 18px;
        line-height: 1.5;
      }

      .chuck-grid {
        display: grid;
        grid-template-areas:
          "header header"
          "feed favorites"
          "footer footer";
        grid-column-gap: 50px;
        grid-template-columns: 1fr 1fr;
        grid-template-rows: auto auto;
      }

      .chuck-grid__header {
        grid-area: header;
      }

      .chuck-grid__feed {
        grid-area: feed;
      }

      .chuck-grid__favorites {
        grid-area: favorites;
      }

      .chuck-grid__footer {
        grid-area: footer;
      }

      .joke-list {
        list-style: none;
        margin: 0;
        padding: 7px;
        background: rgba(0,0,0,.01);
        border: 1px solid rgba(0,0,0,.2);
        border-radius: 5px;
      }

      .joke {
        list-style: none;
        display: flex;
        margin-bottom: 7px;
        border-bottom: 1px solid rgba(0,0,0,.2);
      }

      .joke__text {
        flex-grow: 1;
        padding: 11px 0;
      }

      .joke__add, .joke__add-icon {
        min-width: 40px;
        min-height: 40px;
        width: 40px;
        height: 40px;
      }

      .joke__add {
        border: none;
        appearance: none;
        box-shadow: none;
        background: none;
        padding: 0;
        outline: none;
        align-self: center;
        margin-right: 10px;
      }

      .joke__add-icon--added {
        fill: red;
        stroke: red;
      }
    </style>
    <div id="root">
      Chuck's getting everything ready, hold on...
    </div>

    <script src="bundle.js"></script>
  </body>
</html>
